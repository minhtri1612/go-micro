# Bootstrap overrides for workload clusters (dev/staging/prod) on fresh Kind.
# Use this file until monitoring CRDs (ServiceMonitor) exist in each workload cluster.

hubble:
  metrics:
    serviceMonitor:
      enabled: false

prometheus:
  serviceMonitor:
    enabled: false

operator:
  prometheus:
    serviceMonitor:
      enabled: false
