# Peer hub — cập nhật bằng: HUB_CTX=<kubectl-context-management> ./scripts/kind-clustermesh-peer-ip.sh
# 203.0.113.1 = TEST-NET (RFC 5737), không dùng cho traffic thật; Argo sync Cilium spoke cần IP/NLB thật sau script.
clustermesh:
  config:
    clusters:
      - name: management
        port: 2379
        ips:
          - 203.0.113.1
