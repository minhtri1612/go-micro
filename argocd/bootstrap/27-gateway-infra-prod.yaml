apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway-infra-prod
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: prod
  sources:
    - repoURL: https://github.com/minhtri1612/go-micro.git
      path: template
      targetRevision: main
      helm:
        releaseName: gateway-infra
        valueFiles:
          - $values/argocd/helm/gateway-infra-prod.yaml
    - repoURL: https://github.com/minhtri1612/go-micro.git
      targetRevision: main
      ref: values
  destination:
    name: prod
    namespace: microservices-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
