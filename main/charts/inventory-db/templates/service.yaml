apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventory-db.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "inventory-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "inventory-db.selectorLabels" . | nindent 4 }}