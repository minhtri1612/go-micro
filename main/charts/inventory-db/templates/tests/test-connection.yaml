apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inventory-db.fullname" . }}-test-connection"
  labels:
    {{- include "inventory-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inventory-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
