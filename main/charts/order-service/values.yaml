replicaCount: 2

httpRoute:
  enabled: false
  annotations: {}
  parentRefs:
    - name: external-gateway
      namespace: istio-ingress

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80


orderService:
  name: order-service-deployment
  namespace: go-micro
  database:
    host: "go-micro-main-order-db"
    port: "5432"
    name: "order_db"
    user: "postgres"
    password: "canh177"
  redis:
    host: "go-micro-main-redis"
    port: "6379"
  rabbitmq:
    host: "go-micro-main-rabbitmq"
    port: "5672"
    user: "guest"
    password: "guest"

workload:
  image: 398045402467.dkr.ecr.ap-southeast-2.amazonaws.com/order-service
  tag: "latest" # Using ECR image
  resources:
    requests:
      memory: 256Mi
      cpu: 128m
    limits:
      memory: 512Mi
      cpu: 500m
  ports:
    containerPort: 8081

service:
  type: ClusterIP
  port: 8081
  targetPort: 8081

ingress:
  enabled: false

serviceAccount:
  create: false

imagePullSecrets:
  - name: ecr-secret
