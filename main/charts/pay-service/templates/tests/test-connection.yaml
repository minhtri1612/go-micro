apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pay-service.fullname" . }}-test-connection"
  labels:
    {{- include "pay-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pay-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
