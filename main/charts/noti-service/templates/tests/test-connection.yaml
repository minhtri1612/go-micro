apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "noti-service.fullname" . }}-test-connection"
  labels:
    {{- include "noti-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "noti-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
