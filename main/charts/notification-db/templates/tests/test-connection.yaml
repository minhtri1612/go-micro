apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notification-db.fullname" . }}-test-connection"
  labels:
    {{- include "notification-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notification-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
