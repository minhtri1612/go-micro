env: staging
projectName: go-micro

routing:
  provider: traefik

traefik:
  enabled: true
  hostname: staging.go-micro.local
  stableWeight: 90
  canaryWeight: 10
  darkLaunch: true   # Staging: QA team dùng header X-Canary: true để test

ciliumNetworkPolicy:
  enabled: false
  authenticationMode: ""

rollout:
  enabled: true
  analysis:
    enabled: true
    prometheusAddress: http://172.18.0.3:32090
    templates:
      # Gate 1: Smoke Test
      - name: smoke-test
        args:
          - name: service-name
        metrics:
          - name: health-check
            count: 1
            failureLimit: 0
            provider:
              job:
                spec:
                  backoffLimit: 0
                  template:
                    spec:
                      restartPolicy: Never
                      containers:
                        - name: smoke
                          image: curlimages/curl:8.5.0
                          command:
                            - sh
                            - -c
                            - |
                              set -e
                              SVC="{{ "{{" }}args.service-name{{ "}}" }}"
                              echo "=== Smoke Test: $SVC ==="
                              STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 http://$SVC/health 2>/dev/null || echo "000")
                              if [ "$STATUS" = "000" ]; then
                                echo "FAILED: Cannot reach $SVC"
                                exit 1
                              fi
                              if [ "$STATUS" -ge 500 ]; then
                                echo "FAILED: $SVC returned HTTP $STATUS"
                                exit 1
                              fi
                              echo "PASSED: $SVC returned HTTP $STATUS"

      # Gate 2: Synthetic Load - 100 req x 0.5s = ~50s, max 5% errors
      - name: synthetic-load
        args:
          - name: service-name
        metrics:
          - name: load-test
            count: 1
            failureLimit: 0
            provider:
              job:
                spec:
                  backoffLimit: 0
                  template:
                    spec:
                      restartPolicy: Never
                      containers:
                        - name: synthetic
                          image: curlimages/curl:8.5.0
                          command:
                            - sh
                            - -c
                            - |
                              SVC="{{ "{{" }}args.service-name{{ "}}" }}"
                              ERRORS=0; TOTAL=100
                              echo "=== Synthetic Load: $SVC (${TOTAL} requests) ==="
                              for i in $(seq 1 $TOTAL); do
                                STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 \
                                  http://$SVC/health 2>/dev/null || echo "000")
                                if [ "$STATUS" -ge 500 ] || [ "$STATUS" = "000" ]; then
                                  ERRORS=$((ERRORS + 1))
                                fi
                                sleep 0.5
                              done
                              echo "Result: $ERRORS/$TOTAL errors"
                              if [ "$ERRORS" -gt 5 ]; then
                                echo "FAILED: error rate $(( ERRORS * 100 / TOTAL ))% > 5%"
                                exit 1
                              fi
                              echo "PASSED: synthetic load completed"

      # Gate 3: Success Rate - HTTP error rate thật (staging: ngưỡng 0.90)
      - name: success-rate
        args:
          - name: service-name
        metrics:
          - name: success-rate
            interval: 30s
            count: 8
            initialDelay: 60s
            failureLimit: 2
            successCondition: result[0] >= 0.90
            failureCondition: result[0] < 0.90
            provider:
              prometheus:
                address: '{{ .Values.rollout.analysis.prometheusAddress | default "http://172.18.0.3:32090" }}'
                query: |
                  (
                    1 - (
                      sum(rate(gin_uri_request_total{pod=~"{{ "{{" }}args.service-name{{ "}}" }}-.*",code=~"5.."}[2m])) or vector(0)
                      /
                      (sum(rate(gin_uri_request_total{pod=~"{{ "{{" }}args.service-name{{ "}}" }}-.*"}[2m])) or vector(1))
                    )
                  ) or vector(1)
  strategy: canary
  canary:
    steps:
      # Wave 1: 5% - Smoke test
      - setWeight: 5
      - analysis:
          templates:
            - templateName: '{{ include "template.fullname" . }}-smoke-test'
          args:
            - name: service-name
              value: '{{ include "template.fullname" . }}'
      # Wave 2: Synthetic load - warm up + tạo Prometheus data
      - analysis:
          templates:
            - templateName: '{{ include "template.fullname" . }}-synthetic-load'
          args:
            - name: service-name
              value: '{{ include "template.fullname" . }}'
      # Wave 3: 20% - Prometheus gate chặt hơn, pause lâu hơn
      - setWeight: 20
      - pause:
          duration: 5m
      - analysis:
          templates:
            - templateName: '{{ include "template.fullname" . }}-success-rate'
          args:
            - name: service-name
              value: '{{ include "template.fullname" . }}'
      # Wave 4: Promote
      - setWeight: 50
      - setWeight: 100
  blueGreen:
    activeService: ""
    previewService: ""
    autoPromotionEnabled: true
    scaleDownDelaySeconds: 30

product:
  namespace: microservices-staging
  secrets:
    target:
      name: go-micro-product-secrets-staging
    remoteKey: go-micro/staging/app-credentials

inventory:
  namespace: microservices-staging
  secrets:
    target:
      name: go-micro-inventory-secrets-staging
    remoteKey: go-micro/staging/app-credentials

order:
  namespace: microservices-staging
  secrets:
    target:
      name: go-micro-order-secrets-staging
    remoteKey: go-micro/staging/app-credentials

noti:
  namespace: microservices-staging
  secrets:
    target:
      name: go-micro-noti-secrets-staging
    remoteKey: go-micro/staging/app-credentials

payment:
  namespace: microservices-staging
  secrets:
    target:
      name: go-micro-payment-secrets-staging
    remoteKey: go-micro/staging/app-credentials
